You are an expert scientific paper analyst. Below is a JSON array of model outputs for the **same** PDF. Each item has:
- "provider", "model_name" (which system produced it)
- "extraction": the structured fields each model returned, including **field_confidence** when present.

**Using field_confidence:** Each model may attach per-field scores (typically 0.0–1.0) reflecting how sure it is about that field. Use these as **signals** when models disagree: prefer values from higher-confidence fields when they align with plausibility and cross-model agreement; treat very low confidence as a warning to verify against other models or consensus. Confidence is **not** ground truth—combine it with agreement, internal consistency, and scholarly norms.

Your job: compare the extractions and return **one** best judgment as JSON.

**Output format (single object, valid JSON only, no markdown):**

Use the **same keys** as the paper fields inside "extraction" (authors, title, journal_name, volume_issue_pages, year_of_publication, doi, pubmed_hyperlink, study_design, study_question, population_assessed, follow_up_duration, outcome_measures, inclusion_criteria, number_of_participants_in_study, answer_to_study_question). Values are your chosen or merged best answers.

Do **not** include **field_confidence** in your output—the final consensus is stored without per-field model confidence.

Also include:
- "reasoning" (string): concise explanation; you may reference which models or confidence patterns informed key choices.
- Optionally "agreement_score" (number 0.0–1.0): overall agreement among models.
- Optionally "field_level_decisions" (object): short per-field notes if useful.

Rules:
- "study_design" must be one of the same string enum values as in the inputs (e.g. "clinical_trial", "case_study_uncontrolled").
- Prefer consensus; when models disagree, weigh **field_confidence**, agreement, and accuracy.
- **Every string field value must be 20 words or fewer.** Count carefully; truncate or summarize to stay within the limit. Numeric fields (year, participant count, agreement score) are exempt.

Model outputs (JSON array):

$responses_json
